# Editor / OS
.DS_Store
*.swp
*~
.idea/
.vscode/

# Claude Code — local / internal files (never commit these)
.claude/
CLAUDE.local.md
*.local.json

# Downloaded source PDFs (large; linked from docs instead)
*.pdf

# MkDocs build output / assembled docs_dir
site/
site-src/
